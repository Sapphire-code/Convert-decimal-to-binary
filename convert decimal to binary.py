vara = "x"
print("x to close the program")
decimal = input("Enter any number(s) in decimal form: ")
if decimal == vara:
                exit()

else:
                decimal = int(decimal);
                print(decimal, "in binary = ", bin(decimal).replace('0b', ''))
input("Type x to close the program: ")
                
